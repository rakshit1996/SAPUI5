<mvc:View
controllerName = "ui5.walkthrough.controller.InvoiceList"
xmlns =  "sap.m"
xmlns:core= "sap.ui.core"
xmlns:mvc = "sap.ui.core.mvc">
<List 
    id="InvoiceList"
    width="auto"
    class= "sapUiResponsiveMargin"
    items="{
        path:'invoice>/Invoices',
        sorter:{
                path:'ShipperName',
                group :true
                }
    }">
    <headerToolbar >
        <Toolbar>
        <Image src="images/sap-logo.svg" width="50px" height="60px"/>
        <Title text="{i18n>invoiceListTitle}"></Title>
        <ToolbarSpacer/>   <!--this is used to seperate the toolbar text and filter filed-->
        <SearchField 
               width="20%" 
               search=".onFilterInvoices"/>
        </Toolbar>
    </headerToolbar>
    <items>
       <ObjectListItem  
                    core:require= "{
                        Currency :'sap/ui/model/type/Currency'
                    }"
                    title="{invoice>ProductName} X {invoice>Quantity} "
                    number= "{
                             parts:[
                             'invoice>ExtendedPrice',
                             'view>/currency'
                             ],
                             type:'Currecy',
                             formatOptions:{showMeasure: false}
                             }"
                    numberUnit= "{view>currency}"
                    numberState="{= ${invoice>ExtendedPrice}> 50 ? 'Error': 'Success'}"
                    type="Navigation"
                    press=".onPress" >
                <firstStatus>
                    <ObjectStatus
                        core:require="{
                            Formatter: 'ui5/walkthrough/model/formatter'
                        }"
                        text="{
                            path: 'invoice>Status',
                            formatter: 'Formatter.statusText.bind($controller)'
                        }"/>
                </firstStatus>
       </ObjectListItem>
    </items> 
</List>
</mvc:View>
